export default {
    Path: {
        rootDef: process.cwd() + '/definition',
        modelDef: process.cwd() + '/definition/models',
        distModelDef: process.cwd() + '/dist/definition/models'
    },
    Mongo: {
        server: 'mongodb://localhost/product'
    }
};
